[package]
name = "tune_stream"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
hypersync-client = { path = "../../hypersync-client" }

tokio = { version = "1", features = ["full"] }
serde_json = "1"
anyhow = "1.0.100"
env_logger = "0.11"
log = "0.4"
